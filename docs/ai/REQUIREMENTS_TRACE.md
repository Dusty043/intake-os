# AI Requirements Trace Pointer

The authoritative implementation trace currently lives in:

- `docs/product/requirements-trace.md`

This file exists because the build guide expects an AI memory requirements trace under `docs/ai/`. Keep product requirement statuses in the product trace unless the team later decides to split agent-only tracking from product behavior tracking.
